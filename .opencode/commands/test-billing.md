---
description: Run the Flaxia billing test suite
---

Run the Flaxia billing test suite and report any failures.

1. If the test server is not already running on port 8788, start it in the
   background with `npm run dev:test`.
2. Run `npm run test:billing`.
3. Summarize failures with the relevant assertion output and likely cause.
