---
description: Verify the current changes (typecheck, lint, tests)
---

Verify the current changes and report problems with file paths and line numbers.

1. `npm run typecheck`
2. `npm run lint`
3. Run the most relevant test suite (e.g. `npm run test:billing`).

Do not fix anything yet — report findings first.
