・プロフィールから見るそのアカウントのポストだと参照返信でも親ポスト扱いになって>>のリンクが機能しないバグを修正。

・通知ページにいるときだけナビゲーションバーの通知数の数だけが消えるて既読ボタンを押しても数字だけが消えて後ろの緑色が残るバグを修正。

・プロフィールの画像を変更できない(画像を選択したのにそれが編集モーダルで反映されない)バグを修正。

・親ポストに対する返信数のなかに参照返信の返信数が入らないバグを修正。

・ポストでメンションをする際に@を打ってそのあと文字を打つとアカウントのサジェストを表示する機能を実装。

・ポストで投票機能を使えるように実装。
